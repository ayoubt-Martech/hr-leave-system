/**
 * Shared UI-facing constants that mirror business rules enforced server-side
 * (see server/routes/requests.ts). Keep these in sync with the backend if the
 * policy ever changes there.
 */
export const MAX_PENDING_REQUESTS = 2;
